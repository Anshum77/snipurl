## 🚧 Current Status: Phase 1 Complete

✅ FastAPI backend with auto-generated docs  
✅ URL shortening with random 6-char codes (Base62)  
✅ HTTP 307 redirects  
✅ In-memory storage (PostgreSQL integration coming next)

**Run locally:**
```bash
pip install -r requirements.txt
uvicorn app.main:app --reload